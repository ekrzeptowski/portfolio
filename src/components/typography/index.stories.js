import React from "react";
import { SectionTitle as Section_Title } from ".";

export default {
  title: "Typography",
  // component: SectionTitle
};
export const SectionTitle = (args) => <Section_Title {...args} />;
SectionTitle.args = { children: "Section title" };
{
  /* Section.arg */
}
